akka {
  actor {
    provider = "akka.remote.RemoteActorRefProvider"
  }

  remote {
    netty.tcp {
      hostname = "127.0.0.1"
      port = 10001
    }
  }

  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = INFO
}